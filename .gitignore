# Jekyll build output
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
_preview/

# Bundler
vendor/
.bundle/

# Optional: Gemfile.lock
# Uncomment the line below if you want to ignore Gemfile.lock
# (GitHub Pages typically commits it for consistency, but you can ignore it if using local vendor installs)
# Gemfile.lock

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/

# Node modules (if you add any node tooling later)
node_modules/

# Local environment and secret files
.env
.env.*
!.env.example
!.env.*.example

# Private keys, certificate bundles, and keystores
*.key
*.pem
*.p12
*.pfx
*.jks
*.keystore

# Common credential files
credentials.json
service-account*.json
secrets.json
